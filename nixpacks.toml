[phases.setup]
nixPkgs = ["playwright-driver", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "playwright install chromium",
  "playwright install-deps"
]

[start]
cmd = "hypercorn turnstile_solver:app -b 0.0.0.0:${PORT:-5000}"